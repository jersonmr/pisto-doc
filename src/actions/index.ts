import { loginAction } from "./login.action";
import { logoutAction } from "./logout.action";
import { loginGoogleAction } from "./login-google.action";

export const server = { loginAction, logoutAction, loginGoogleAction };
